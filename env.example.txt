# ============================================
# 数据库配置（选择一种数据库，取消注释对应的配置）
# ============================================

# PostgreSQL (推荐)
DATABASE_URL=postgresql://user:password@localhost:5432/document_qa
# 或者使用 psycopg2
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/document_qa

# MySQL
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/document_qa
# 或者使用 mysqlclient
# DATABASE_URL=mysql://user:password@localhost:3306/document_qa

# SQLite (开发测试用，生产环境不推荐)
# DATABASE_URL=sqlite:///./document_qa.db
# 或者使用绝对路径
# DATABASE_URL=sqlite:////absolute/path/to/document_qa.db

# Oracle
# DATABASE_URL=oracle+cx_oracle://user:password@localhost:1521/xe
# 或者使用 oracle://
# DATABASE_URL=oracle://user:password@localhost:1521/xe

# ============================================
# JWT配置
# ============================================
SECRET_KEY=your-secret-key-change-in-production-use-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# 应用配置
# ============================================
PROJECT_NAME=智能文档问答系统
API_V1_PREFIX=/api/v1

